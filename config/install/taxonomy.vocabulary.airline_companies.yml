langcode: en
status: true
dependencies:
  enforced:
    module:
      - travel_planner
name: Airline Companies
vid: airline_compaines
description: 'Stores the airline companies for the Travel Planner module'
hierarchy: 0
weight: 0
